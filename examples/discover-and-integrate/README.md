# Discover and Integrate

Discover the ProofOfVerdict Judge via Agent0 (ERC-8004) and call the API.

## Run

```bash
BASE_SEPOLIA_RPC=https://base-sepolia.g.alchemy.com/v2/YOUR_KEY npm start
```

Optional: `JUDGE_URL` as fallback if discovery fails.
